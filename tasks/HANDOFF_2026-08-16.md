# Handoff — Kino-app сессия 2026-08-16

## Что сделано в этой сессии (в проде)

1. **PR #39** merged — фикс safe-area для крестика в модалках History и Watchlist preview (`safe-top` класс).
2. **PR #41** merged — подсветка кнопок Смотрю!/Оценить в карточке title-search (проверка через `getMovieDedupKey` по `customMovies`/`watched`).
3. **PR #42** merged — поле «Поиск по названию…» в табе История (`WatchHistory.tsx`).

Ветка `claude/rate-limiting-audit-fixes-BP00z` — все три PR через неё, **все смержены вручную в main** через `mcp__github__merge_pull_request` (squash). Automerge.yml не срабатывает на события от MCP-токена (защита GitHub Actions от рекурсии), поэтому draft-шаг бесполезен — создавай PR сразу не-draft и мержи.

---

## Открытые проблемы (не тронуты, требуют решения)

### 🔴 SHOWSTOPPERS — сломано в проде прямо сейчас

**1. DeepSeek `deepseek-chat` отключён провайдером 24 июля 2026, 15:59 UTC.**
- `supabase/functions/movie-recommendation/index.ts:7` — единственный провайдер функции, фолбэков нет. Подбор фильмов, вероятно, лежит с 24 июля.
- `supabase/functions/ai-chat/index.ts:10` — фолбэк если provider=deepseek.
- Миграция: `deepseek-v4-flash` (прямой преемник, цена та же). **Не** `v4-pro` — втрое дороже.
- **Спасает только если** в Supabase Secrets задан override `DEEPSEEK_MODEL`. Проверить в дашборде.

**2. Anthropic-организация владельца закрыта.**
- `ANTHROPIC_API_KEY` может быть нерабочим. Дефолт `ai-chat` — `"claude"` (`index.ts:280`).
- Пользователь месяц назад запросила отзыв — без результата, считаем окончательно.
- Планировали сменить дефолт на `gpt4o`, но не сделали — переключились на пересчёт цен.

**3. Supabase `SECURITY DEFINER` views (advisor ERROR).**
- `public.chat_messages`, `public.user_movies` — не уважают RLS, любой залогиненный видит чужое.
- **Views нет в `supabase/migrations/`** — созданы вне git через SQL Editor.
- Фикс — по одной строке на каждый: `ALTER VIEW ... SET (security_invoker = on);`
- **Пользователь отклонила SELECT-запрос** на чтение определений views — без её разрешения на чтение не двигаемся.

### 🟡 Важное но не срочное

**4. `gemini-2.5-flash` снимается 16 октября 2026.**
- `ai-chat/index.ts:12`. Преемники: `gemini-3.1-flash-lite` ($0.25/$1.50) или `gemini-3.7-flash` ($0.75/$3.75).

**5. `claude-sonnet-4-6` устарел и дороже.**
- `ai-chat/index.ts:13` — $3/$15. Актуальный `claude-sonnet-5` — $2/$10 (повышение с 1 сентября отменено).

**6. Supabase advisor WARN:**
- `kino.rate_limits` и `public.rate_limits` — RLS on, политик нет (INFO).
- `public.check_rate_limit` и `kino.check_and_increment_rate_limit` — дубли в двух схемах, обе доступны `anon` через REST. Возможен мёртвый код.
- Leaked-password protection в Supabase Auth выключена.

**7. Полный аудит не завершён.**
- Запустили `repo-auditor` (agent id `ad6a93689684913ad`), но остановили по команде «стоп». Отчёт не получен.

---

## Пересчёт цен (для памяти)

За 1000 запросов (3000 in / 1200 out токенов):

| модель | цена | статус |
|---|---|---|
| **deepseek-v4-flash** | $0.76 | ✅ преемник |
| **gpt-4o-mini** | $1.17 | ✅ дешевле всех |
| gemini-3.1-flash-lite | $2.55 | ✅ |
| GPT-5.6 Luna | $10.20 | ✅ |
| **claude-sonnet-5** | $18.00 | 🔒 нужен доступ |
| **gpt-4o** (выбор пользователя) | $19.50 | ✅ работает |
| claude-sonnet-4-6 (в коде) | $27.00 | 🔒 устарел + Anthropic закрыт |

**OpenRouter** — прокси-агрегатор, 5.5% наценка, OpenAI-совместимый API. `callOpenAICompat` в коде уже принимает base URL параметром — нужно вынести в env. Легальный путь к Claude без своего Anthropic-аккаунта.

---

## Контекст среды

- **Репо:** `github.com/arsid0305/kino-app`, локально `/home/user/Kino-app`
- **Supabase проект:** `ovhwxfdtkzwxfomdlgjv`
- **API-ключи** (в Supabase Secrets): `ANTHROPIC_API_KEY` (🔒 бан), `OPENAI_API_KEY`, `GOOGLE_API_KEY`, `DEEPSEEK_API_KEY`, `ALLOWED_ORIGINS`
- **Фронт:** Vercel, автодеплой при push в main
- **Функции:** GitHub Actions `deploy.yml` при изменении `supabase/functions/**`

### Особенности среды сессии
- `Supabase CLI`, `Deno`, реальный `.env` — недоступны.
- **Automerge не работает** через MCP-токен (событие ready_for_review от App не триггерит workflows). Все мержи — вручную через `merge_pull_request`.
- **Прямой доступ к докам провайдеров** (platform.openai.com, api-docs.deepseek.com) заблокирован сетевым прокси. Только `WebSearch`.
- **llm_wiki недоступен** из этой сессии — скоуп репо ограничен `template`, `ai_os`, `kino-app`.

---

## Начальный промт для новой сессии

```
Контекст переношу с прошлой сессии — читай /home/user/Kino-app/tasks/HANDOFF_2026-08-16.md.

Первым делом:
1. Проверь `DEEPSEEK_MODEL` в Supabase Secrets — от этого зависит, лежит ли movie-recommendation.
2. Решение по плану: (a) миграция deepseek-chat → deepseek-v4-flash, (b) смена дефолта ai-chat с claude на что-то живое, (c) fix SECURITY DEFINER views.

Работать на новой ветке `claude/audit-fixes-<slug>`, PR помечать draft и мержить вручную (automerge через MCP-токен не работает).

Не трогай ANTHROPIC/claude-код — оставляем на случай восстановления доступа.
```

## MCP-серверы

Не настраиваются в репо — привязаны к аккаунту claude.ai. В новой сессии подтянутся автоматически: GitHub, Supabase, Vercel, WebSearch — всё нужное уже есть.

## Первые команды в новой сессии

```bash
# 1. Синхронизация ветки
cd /home/user/Kino-app
git fetch origin main
git checkout -B claude/audit-fixes-<slug> origin/main

# 2. Проверить советник Supabase — не появилось ли новых ERROR
# (через MCP: get_advisors type=security на проекте ovhwxfdtkzwxfomdlgjv)

# 3. Проверить, работает ли movie-recommendation
# (через Supabase MCP: get_logs / execute_sql на списке ошибок функции)
```
