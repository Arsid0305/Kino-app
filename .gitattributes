# Windows PowerShell 5.1 читает .ps1 без BOM как ANSI (cp1251) — кириллица
# внутри скрипта превращается в мусор и валит парсер. Файл хранится с BOM,
# а этот атрибут держит в рабочей копии CRLF, как ожидает PowerShell.
*.ps1 text eol=crlf
