# TODO

## [2026-09-06 — актуально]

- **Следующий приоритет:** безопасность N01/N03/N04, затем доставка N02/N08 и сохранность данных. Код и мерж — в следующей сессии с Claude по решению Арины.
- **Не доделано:** исправления из аудита не реализованы; production не изменён. Отметки ниже — задачи, не подтверждение исправления.
- **Контекст:** [аудит](../docs/AUDIT_2026-09-06.md), main `595e8dfc81d7e117d25818f84c78958083bb0fc1`, [draft PR #77](https://github.com/Arsid0305/Kino-app/pull/77), ветка `audit/kino-review-2026-09-06`. До мержа читать документы из этой ветки, в main их ещё нет. Новые PR для записей этой сессии не создавать.
- **Решения пользователя:** GitHub-бан снят; приложение открывается через Vercel. Старые заявления об активном бане ниже и в CLAUDE.md не являются текущими фактами. Состояние автодеплоя Vercel отдельно не проверено. Никакого автоматического мержа или деплоя в рамках закрытия этой сессии.
- **Проверено:** 15/15 тестов, build и явные app/node typechecks проходят; полный lint — 14 ошибок/3 предупреждения; consistency — 4 сообщения, скрипт устарел. 8 локальных синтетических воспроизведений; GitHub и Supabase проверялись read-only. Vercel/iOS, OTP, платные AI-вызовы и чистое восстановление БД не проверены.
- **Сохранение знаний:** выводы — в `tasks/lessons.md`; доказательства — в отчёте. Открытый долг ниже сохраняется, его уточнения не считать новыми задачами.

### HIGH — задачи по аудиту

- [ ] **N01 — отключить прямой автомерж и согласовать required checks.** Готово: workflow не вызывает merge/deleteRef, обычный PR не сливается без решения пользователя; не проверять уязвимость реальным опасным PR. Одна настройка native auto-merge не устраняет прямой API-мерж.
- [ ] **N02/N08 — восстановить достоверный gate деплоя.** Готово: SHA `supabase/setup-cli` существует; явно проверяются app/node configs и ненулевое число тестов, lint; у deploy есть минимальные permissions, timeout, concurrency, фиксированная версия CLI и нужные paths включая config.toml. Один согласованный проверочный запуск, без самовольного production-деплоя.
- [ ] **N03/N04 — закрыть лишние права RPC и проверить legacy RLS.** Готово: ACL обеих `check_and_increment_rate_limit` допускают только серверную роль; назначение старых kino-таблиц и экспозиция Data API установлены, доступ ограничен по владельцу/назначению. Проверить отрицательные сценарии anon/чужого пользователя; данные не удалять вслепую. Старую `public.check_rate_limit` см. существующий долг ниже.
- [ ] **N05 — согласовать клиентский контекст с серверными лимитами.** Готово: тесты 19/20/21 сообщений, 2000 на сообщение и 12000 суммарно; длинный разговор можно продолжить без удаления сохранённой истории.
- [ ] **N06 — подтверждать облачные записи и ожидать импорт.** Готово: отказ add/rate/delete/import не показывает ложный успех; rollback либо явное несохранённое состояние, Promise callback ожидается; повторная синхронизация не теряет подтверждённые изменения. Атомарность пачек — существующая задача ниже.
- [ ] **N07 — воспроизводимые миграции Kino.** Готово: baseline и forward-миграции поднимают нужную схему на отдельной чистой БД; устранены несовместимые rate_limits; объекты Technical Language не переносятся. Production reset запрещён.
- **N09 — дополнить существующую задачу валидации ниже**, не заводить второй чекбокс.

### MEDIUM — задачи по аудиту

- [ ] **N10 — проверять результат удаления чата.** Готово: `{error}` от Supabase не скрывается, локальная история очищается после успеха; проверены отказ и конкурирующая отправка.
- [ ] **N11 — разделить кеш по аккаунтам.** Готово: signout/смена пользователя сбрасывает state и старые запросы; при неудачном sync аккаунт B не видит историю A; локальный режим отделён.
- [ ] **N12/N13/N14 — единая нормализация и честный импорт.** Готово: двойные пробелы не обходят запрет повторов; film/series/miniseries одинаковы во всех форматах; 5001 запись даёт явный результат об ограничении; дата 1970 не попадает в Recent taste. Режиссёры XLSX — существующая задача ниже.
- [ ] **N15 — ограничить время AI-запросов.** Готово: клиентские и серверные deadlines, retries только временных отказов; явный результат таймаута; без молчаливой смены платного провайдера.
- [ ] **N16 — актуализировать инструкции и consistency-check.** Готово: снятый бан отражён без выводов из отсутствия runs, README с реальными путями/endpoints; нет взаимоисключающих инструкций о мерже/fallback. Core rules менять в AI_OS SSOT с последующей синхронизацией, не локальной правкой копии. Изменения других репо в этой сессии не выполнялись.
- [ ] **N17 — проверить потребителей legacy endpoints.** Готово: назначение openai-chat/deepseek-chat установлено, ненужные отключены только после согласования; для оставшихся проверены auth, CORS, rate limit, безопасные ошибки. `verify_jwt=false` само по себе не доказывает отсутствие внутреннего getUser.
- [ ] **Доступность UI и единый manifest.** Готово: модалки доступны с клавиатуры, имеют focus/Escape/dialog; иконки подписаны, история доступна без мыши, масштабирование разрешено; build содержит один manifest. Визуальные изменения сверить с design-system, submodule автоматически не обновлять.
- [ ] **Диагностика отказов.** Готово: безопасный request-id связывает запрос и платформенные логи; отказ БД rate-limit отличим от исчерпания квоты, секреты/ответы провайдера не уходят клиенту.
- [ ] **Закрыть пробелы production-проверки.** Готово: согласованно проверены Vercel SHA/env/CORS/PWA/iOS, Auth/OTP (в UI ещё допустимы 6–7 цифр), доступность моделей в аккаунтах и бюджет. Не считать результаты статического аудита runtime-проверкой.

### LOW — после основных исправлений

- [ ] Проверить unused dependencies и кандидатов App.css/ui/dialog/ui/toggle; удалять только после проверки импортов и сборки. Не приравнивать установленный пакет к включённому в bundle. Index/AiAdvisor не переписывать: выделять sync/auth только при соответствующем исправлении.
- [ ] Перед ручным деплоем проверять exit code checkout и использовать безопасное fast-forward обновление; сохранить BOM/CRLF PowerShell. Проверить сценарии на Windows без production-деплоя.

### История и оставшийся долг

Секции августа ниже — исторические итоги, не текущий статус инфраструктуры. Незакрытые задачи остаются в «Открытых задачах»; не переносить их копиями. Единственная актуальная сессионная метка — выше.

## Сессия 2026-08-16 — прод чинили, нашли давние поломки ✅

### Прод
- [x] `deepseek-chat` → `deepseek-v4-flash` в обеих функциях — провайдер отключил модель 24 июля, подбор фильмов лежал с тех пор
- [x] Дефолтный provider `ai-chat`: `claude` → `gpt4o` (Anthropic-организация закрыта; ветка `case "claude"` сохранена на случай восстановления доступа)
- [x] Обе edge functions задеплоены через Supabase MCP (`ai-chat` v32, `movie-recommendation` v36)
- [x] `SECURITY DEFINER` на `public.chat_messages` и `public.user_movies` → `security_invoker` (миграция `20260816120956`). Advisor `security_definer_view` очищен: до этого любой залогиненный видел чужие записи

### Импорт и синхронизация
- [x] **`.limit(500)` заменён на постраничное чтение.** Библиотека выросла до 1193 строк, доезжало 500: из 593 оценок пользователь видела 241. На компе баг маскировался localStorage, на телефоне вылезал в чистом виде
- [x] Импорт больше не делает `Promise.all` по каждому фильму — на 200 строках это было ~400 конкурентов за один auth-лок supabase-js («Lock was released because another request stole it») и столько же запросов. Теперь пачками по 100
- [x] Дедупликация игнорирует тип: «Корона (film)» и «Корона (series)» — один сериал. Импорт с расходящимся типом создал 58 дублей
- [x] База дочищена: 539 / 592 / 5, дублей нет. Бэкапы — `kino.watchlist_dupes_backup_20260816`, `kino.dupes_backup_20260816_v2`

### Инфраструктура
- [x] Обнаружено: **Vercel не деплоил 51 день** (последний деплой 26 июня). Флаг T&S на аккаунте выключил не только Actions, но и GitHub-интеграции Vercel и Supabase
- [x] `scripts/deploy.ps1` — ручной деплой одной командой взамен пропавшего автодеплоя
- [x] CLAUDE.md приведён к реальности (там было написано, что Vercel работает)

### UI
- [x] Липкая шапка уезжала под часы и значки iOS при скролле — отступ safe-area переехал с `body` на саму шапку

### Из аудита (второй заход, уже сделано)
- [x] **Дефолт AI-провайдера во фронте** `AiAdvisor.tsx:140` — `claude` → `gpt4o`. Утренний фикс на бэке не срабатывал: фронт всегда шлёт валидное значение (PR #63)
- [x] **Фолбэк OpenAI для `movie-recommendation`** — один провайдер = одна точка отказа, повторение июльской истории заблокировано (PR #64)
- [x] **Утечка деталей провайдера клиенту** — `error.message` возвращала тело ответа апстрима с id организации и квотами. Теперь только в `console.error`, наружу обобщённое сообщение (PR #64)
- [x] **CORS**: `localhost` разрешён только по флагу `ALLOW_LOCALHOST_CORS=1`, `FALLBACK_ORIGINS` заменены на реальные три домена Vercel, добавлен `Vary: Origin` (PR #65)
- [x] **Rate limit fail-open → fail-closed**: отказ таблицы больше не снимает лимит на платные LLM-вызовы (PR #65)
- [x] **`MAX_PAGES=40`**: при упоре в потолок бросается ошибка вместо молчаливого обрезания на 20 000 записей (PR #65)
- [x] **`create_rate_limit_function` вернули в git** — миграция была применена вручную и не попала в репо (PR #66). Три другие «пропавшие» — из чужой схемы `technical_language` соседнего проекта, не тащим

---

## Сессия 2026-08-17 — синхронизация ✅

- [x] **Тумбстоуны не нужны — облако стало источником правды.** `Index.tsx` при заходе больше не мержит cloud + localStorage и не заливает разницу через `seedCloudLibrary` (PR #68). Удалённое на одном устройстве больше не воскресает на другом. `seedCloudLibrary` вычищена из `supabaseMovieStore` как мёртвый код. Ограничение: офлайн-добавления больше нет — пользователь подтвердила, не критично

---

## Открытые задачи

### Хвост сессии 2026-09-04/05 — на следующую сессию

- [ ] **Иконки провайдеров на главной** — в селекторе подбора (`Index.tsx`) сейчас только текст «Claude / GPT / Gemini / DS». В `AiAdvisor.tsx:55` уже есть готовый компонент `ProviderIcon` с 4 SVG (звёздочка Claude, зелёный OpenAI, ромбик Gemini, круг DS) — переиспользовать: вынести в `src/components/ProviderIcon.tsx` и подставить в оба места. UX-правка, ~15 мин
- [ ] **Toggle кнопки «Смотрю»** — по аудиту 06.09 кнопка не деактивируется: повторный клик снова вызывает добавление. Второй клик должен снимать из watchlist. Правка `MovieCard` и `handleAddToWatchlist` в `Index.tsx`; проверить оба перехода.
- [ ] **Импорт из xlsx: подтянуть режиссёров.** Сейчас `fileParser.ts:179` пишет `director: ''` жёстко — 574 из 594 фильмов без режиссёра. Читать колонку `Режиссёр` из экспорта Кинопоиска (проверить точное имя колонки в реальном файле). После правки — перезалить импорт, чтобы `tasteProfile` начал использовать «Recurring directors»



- [ ] **Бандл 1.3 МБ одним куском.** Vite ругается на каждой сборке. Тяжёлое (`xlsx`, `framer-motion`) грузится сразу, хотя нужно не всегда — на мобильном интернете это лишние секунды белого экрана при первом заходе. Разбить через `manualChunks` / динамический импорт
- [ ] **Service worker обновляется только со второго захода.** Сейчас после каждого деплоя приходится вручную обновлять дважды или чистить данные сайта. Лечится подпиской на `virtual:pwa-register` с перезагрузкой при появлении новой версии
- [ ] **Vitest на Node 26 иногда выполняет 0 тестов** и выходит с кодом 0 — `deploy.ps1` в такой ситуации напишет «тесты зелёные», хотя не проверено ничего. Добавить проверку, что выполненных тестов больше нуля
- [ ] **21 фильм лежит одновременно в «Просмотрено» и «Буду смотреть».** Отметка просмотренным чистит watchlist, обратной чистки нет. На Кинопоиске похожая картина (24 просмотренных в папке), так что, возможно, это не баг — уточнить у пользователя
- [ ] **N09 — валидация входа и ответа AI в edge functions.** Готово: null в watchedMovies → 400, null-рекомендации и неправильный genre не ломают UI; ограничены тело, названия, filters/forbiddenTitles; схемы проверены негативными тестами. TypeScript cast не заменяет runtime-валидацию.
- [ ] Дозаполнить «Буду смотреть» страницами 2–3 Кинопоиска (первые 200 уже импортированы; фикс дублей в проде, можно заливать)
- [x] **[Устаревший дубль, сверено 2026-09-06] Тумбстоуны для удаления.** Описанный обратный sync уже устранён PR #68; см. итог 17.08. Не реализовывать tombstones для устранённого сценария. Отказы явных записей остаются открыты в N06.
- [ ] **Частичный сбой при импорте `supabaseMovieStore.ts:146-155`.** Падение третьей пачки из шести теряет данные оставшихся пачек И не выполняет чистку списков — фильм остаётся и в «Просмотрено», и в «Буду смотреть». Наружу уходит одна ошибка без указания, сколько успело записаться
- [ ] **Зависимости: XLSX и npm audit.** У xlsx 0.18.5 две advisory; выбрать поддерживаемую поставку/замену с сохранением импорта. Всего audit 06.09: 22 пакета (1 critical/12 high/6 moderate/3 low); critical Vitest относится к UI server, не доказанной дыре статического production. Готово: применимость разобрана, обновления проверены тестами/build; без слепого audit fix --force.
- [ ] **`SECURITY DEFINER` на старой `public.check_rate_limit`** — advisor до сих пор ругается. Реально используется `kino.check_and_increment_rate_limit`, старую можно сносить. Проверить, что никто её не зовёт, — и `DROP FUNCTION`
- [ ] **Eslint: 10 известных any и 4 дополнительные ошибки полного lint.** Готово: `eslint .` проходит; предупреждения отдельно разобраны. Не снимать требование только ради зелёного статуса.

---

## Заметки для Claude

> Приоритет имеет контекст 06.09 сверху: бан снят, мерж и реализация оставлены пользователем для Claude. Нижние указания о заблокированных интеграциях исторические; перед действиями сверить актуальные правила AI_OS. Отсутствие runs не доказывает бан.

- **Деплой фронта — только вручную**, пока флаг T&S на аккаунте GitHub: `.\scripts\deploy.ps1` из `C:\Users\arols\kino-deploy`. После каждого мержа давать пользователю готовый блок команд — правило записано в `CLAUDE.md`
- Edge functions, миграции, SQL, мержи — Claude делает сам через MCP, машина пользователя не нужна
- Ветки `claude/...` со слэшем создаются нормально — прежняя заметка про «GitHub MCP не умеет» неверна
- `ALLOWED_ORIGINS` в Supabase Secrets **используется**: `_shared/cors.ts` читает его, хардкод — только fallback. Прежняя заметка «больше не нужен» неверна
- `.ps1` держать в UTF-8 **с BOM**, иначе PowerShell 5.1 читает кириллицу как ANSI и не парсит файл
- Supabase project ID: `ovhwxfdtkzwxfomdlgjv`
- Vercel: проект `kino-app`, домены `kino-arsid.vercel.app`, `kino-app-arsid.vercel.app`, `kino-app-git-main-arsid.vercel.app`
- Кросс-проектные проблемы (флаг T&S, зеркало репозиториев) — в `AI_OS/MEMORY/tasks/cross-repo-todo.md`
